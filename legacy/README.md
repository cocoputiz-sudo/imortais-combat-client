# Legacy IMORTAIS integration snapshots

**Congelado, histórico, não usar.**

Os arquivos desta pasta são snapshots da fase v0.3/hook installer e existem apenas para consulta histórica.

A integração viva e canônica do IMORTAIS Combat Client está exclusivamente em:

`upstream/AlbionOnline-StatisticsAnalysis/src/StatisticsAnalysisTool/`

Os snapshots usam extensão `.cs.txt` de propósito: não participam de build e não devem ser copiados de volta para o fork.

O antigo `install-hooks.ps1` foi neutralizado e não copia nada para o fork.
